
#----------------------------------------------------------------cp:ppd
# Section managed by cPanel: Password Protected Directories     -cp:ppd
# - Do not edit this section of the htaccess file!              -cp:ppd
#----------------------------------------------------------------cp:ppd
AuthType Basic
AuthName "Protected 'dev.gjones.design'"
AuthUserFile "/home/gjonalpd/.htpasswds/dev.gjones.design/passwd"
Require valid-user
#----------------------------------------------------------------cp:ppd
# End section managed by cPanel: Password Protected Directories -cp:ppd
#----------------------------------------------------------------cp:ppd

#-------------------------------------------------------|
# The following code is used to hide .php and 
# .html extensions from a site url. Because this 
# site operates similar to an SPA, these rules do not 
# work, and are thus disabled. I am leaving them in as
# a reference tool just in case.
#-------------------------------------------------------|

#Options +FollowSymLinks -MultiViews
#RewriteEngine On
#RewriteBase /

#RewriteCond %{THE_REQUEST} ^[A-Z]{3,}\s([^.]+)\.php [NC]
#RewriteRule ^ %1 [R,L,NC]

#RewriteCond %{REQUEST_FILENAME}.php -f
#RewriteRule ^ %{REQUEST_FILENAME}.php [L]

#RewriteCond %{THE_REQUEST} ^[A-Z]{3,}\s([^.]+)\.html [NC]
#RewriteRule ^ %1 [R,L,NC]

#RewriteCond %{REQUEST_FILENAME}.html -f
#RewriteRule ^ %{REQUEST_FILENAME}.html [L]